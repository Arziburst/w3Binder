// Bus
import { reduxConfig } from '../bus/config';

export const selectedSaveRace = () => {
    // todo
    // const config = reduxConfig().config;
    // let result = null;

    // Object.values(config).every((value: any) => {
    //     if (value) {
    //         value.type;
    //     }
    // });
};
